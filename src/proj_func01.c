/**
 * proj_d - projects ray directly onto projector plane.
 *
 * @instance: SDL window and renderer instance
 * @map: address of map
 * @p: address of player
 *
 */
void proj_d(SDL_Instance instance, int rcn, int h)
{
	SDL()
}

/**
 * proj - determines ray to project, and projects it.
 *
 * @instance: SDL window and renderer instance
 * @map: address of map
 * @dw: distance to wall
 *
 */
void proj_i(SDL_Instance instance, int i, int dw)
{
	
}

int special_case(ap)
{
	if (ap == 0 || ap == 90 || ap == 180 || ap == 270)
	{
		/* special case */
	}
}
